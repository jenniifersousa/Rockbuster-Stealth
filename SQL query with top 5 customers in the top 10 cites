An SQL query with the top 5 customers in the top 10 cities who have paid the highest total amounts to Rockbuster Stealth 

SELECT A.customer_id, A.first_name, A.last_name, D.country, C.city
    SUM(E.amount) AS customer_payment
FROM customer A
INNER JOIN address B ON A.address_id = B.address_id
INNER JOIN city C ON B.city_id = C.city_id
INNER JOIN country D ON C.country_ID = D.country_ID
INNER JOIN payment E ON A.customer_id = E.customer_id
WHERE city IN ('Aurora', 'Kuwana', 'Guadalajara', 'So Leopoldo', 'Iwaki', 'Tuguegarao', 'Acua', 'Jakarta', 'Citrus Heights', 'Cianjur')
GROUP BY country, city, A.customer_id
ORDER BY customer_payment DESC
LIMIT 5
